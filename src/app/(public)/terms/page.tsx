import { PublicPlaceholder } from '@/components/layout/public-placeholder';

export default function TermsPage(): React.JSX.Element {
  return (
    <PublicPlaceholder
      description="Review terms of service for authenticated account use and legacy local-data recovery."
      title="Terms"
    />
  );
}
