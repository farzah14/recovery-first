import { PublicPlaceholder } from '@/components/layout/public-placeholder';

export default function HelpPage(): React.JSX.Element {
  return (
    <PublicPlaceholder
      description="Find product guidance and account support entry points."
      title="Help"
    />
  );
}
