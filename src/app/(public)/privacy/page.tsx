import { PublicPlaceholder } from '@/components/layout/public-placeholder';

export default function PrivacyPage(): React.JSX.Element {
  return (
    <PublicPlaceholder
      description="Read how browser-local data and optional account data are protected."
      title="Privacy"
    />
  );
}
