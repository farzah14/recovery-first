import { PublicPlaceholder } from '@/components/layout/public-placeholder';

export default function FeaturesPage(): React.JSX.Element {
  return <PublicPlaceholder description="Explore the product capabilities defined in the approved PRD." title="Features" />;
}
